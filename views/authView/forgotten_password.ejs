<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= appSettings.siteName %> | Forgotten Password</title>
    <!-- Bootstrap CSS -->
    <!-- Custom CSS -->
    <%- include('../snippets/header_meta.ejs') %>

    <style>
      body {
        background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), 
                  url(/images/auth_bg.jpg) no-repeat;
      background-size: cover;
      background-position: center;
      }
      .card {
        background-color: #16242D;
      }
      .form-control, .form-select {
        background-color: #1A2B36;
        color: #fff;
        border: 1px solid #3A4B5C;
      }
      .form-select:focus {
        color: 000 !important;
      }
    </style>

  </head>
  <body>
    <div class="container" style="min-height: 100vh">
      <div class="row justify-content-center">
        <div class="col-md-8 col-lg-5 mb-5">
          <h2 class="card-title text-center mb-3 mt-5"><%= appSettings.siteName %></h2>

          <div class="card mt-5">
            <div class="card-body">
              <h3 class="text-center mb-4">Forgotten Password</h3>
              <p class="text-center mb-4">
                Enter your registered email and get password reset link.
              </p>
              <form id="forgotPasswordForm">
                <div class="mb-4">
                  <label for="email" class="form-label">Email Address *</label>
                  <input
                    type="email"
                    class="form-control"
                    id="email"
                    name="email"
                    placeholder="Enter your email address"
                    required
                  />
                </div>

                <div
                  class="alert alert-danger"
                  id="resetError"
                  style="display: none"
                ></div>

                <div class="d-grid">
                  <button type="submit" class="btn btn--base" id="resetButton">
                    <span
                      class="spinner-border spinner-border-sm d-none"
                      role="status"
                      aria-hidden="true"
                      id="resetSpinner"
                    ></span>
                    Submit
                  </button>
                </div>
              </form>
              <p class="text-center mt-4">
                Remember password?
                <a href="/auth/login" class="base--color"
                  >Login to your account</a
                >
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%- include("../snippets/scripts.ejs") %>
    <script src="/apiClient/apiClient.js"></script>
    <script src="/apiClient/authClient.js"></script>
    <script>
      // Initialize auth pages
      initAuthPages();
    </script>
  </body>
</html>
